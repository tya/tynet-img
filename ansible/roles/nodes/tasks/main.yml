---
# Common tasks for Pi netboot nodes.

- name: Set hostname
  hostname:
    name: "{{ inventory_hostname_short }}"

- name: Ensure ubuntu user SSH authorized keys
  authorized_key:
    user: ubuntu
    state: present
    key: "{{ lookup('file', '~/.ssh/id_ed25519.pub') }}"
